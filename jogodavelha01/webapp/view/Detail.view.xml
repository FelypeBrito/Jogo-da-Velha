<mvc:View
    controllerName="jogodavelha01.controller.Detail"
    xmlns="sap.m"
    xmlns:semantic="sap.f.semantic"
    xmlns:mvc="sap.ui.core.mvc">

    <semantic:SemanticPage
        id="detailPage"
        busy="{detailView>/busy}"
        busyIndicatorDelay="{detailView>/delay}">
        <semantic:titleHeading>
            <Title
                text="{Descricao}"
                level="H2"/>
        </semantic:titleHeading>
        <semantic:headerContent>
            <ObjectAttribute title="{i18n>priceTitle}"/>
            <ObjectNumber />
        </semantic:headerContent>
        <semantic:content>

        <VBox id="vboxConteudoPagina">
        
        <HBox>
        <Button text="{Campo1}" class="sapUiSmallMarginEnd" press="onPressButton"/>
        <Button text="{Campo2}" class="sapUiSmallMarginEnd"  press="onPressButton"/>
        <Button text="{Campo3}"  press="onPressButton"/>
        </HBox>

         <HBox>
        <Button text="{Campo4}" class="sapUiSmallMarginEnd"  press="onPressButton"/>
        <Button text="{Campo5}" class="sapUiSmallMarginEnd"  press="onPressButton"/>
        <Button text="{Campo6}"  press="onPressButton"/>
        </HBox>

         <HBox>
        <Button text="{Campo7}" class="sapUiSmallMarginEnd"  press="onPressButton"/>
        <Button text="{Campo8}" class="sapUiSmallMarginEnd"  press="onPressButton"/>
        <Button text="{Campo9}"  press="onPressButton"/>
        </HBox>

        

            <Table
                id="lineItemsList"
                width="auto"
                items="{ItemJogoSet}"
                updateFinished=".onListUpdateFinished"
                noDataText="{i18n>detailLineItemTableNoDataText}"
                busyIndicatorDelay="{detailView>/lineItemTableDelay}">
                <headerToolbar>
                    <Toolbar>
                        <Title
                            id="lineItemsTitle"
                            text="{detailView>/lineItemListTitle}"
                            titleStyle="H3"
                            level="H3"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="{i18n>detailLineItemTableIDColumn}"/>
                    </Column>
                    <Column
                        minScreenWidth="Tablet"
                        demandPopin="true"
                        hAlign="End">
                        <Text text="{i18n>detailLineItemTableUnitNumberColumn}"/>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <ObjectIdentifier
                                title="{ID}"
                                text="{ID}"/>
                            <ObjectNumber />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
            
        </VBox>
        </semantic:content>
        <!--Semantic ShareMenu Buttons-->
        <semantic:sendEmailAction>
            <semantic:SendEmailAction
                id="shareEmail"
                press=".onSendEmailPress"/>
        </semantic:sendEmailAction>

        <semantic:closeAction>
            <semantic:CloseAction
                    id="closeColumn"
                    press=".onCloseDetailPress"/>
        </semantic:closeAction>

        <semantic:fullScreenAction>
            <semantic:FullScreenAction
                    id="enterFullScreen"
                    visible="{= !${device>/system/phone} &amp;&amp; !${appView>/actionButtonsInfo/midColumn/fullScreen}}"
                    press=".toggleFullScreen"/>
        </semantic:fullScreenAction>
        <semantic:exitFullScreenAction>
            <semantic:ExitFullScreenAction
                    id="exitFullScreen"
                    visible="{= !${device>/system/phone} &amp;&amp; ${appView>/actionButtonsInfo/midColumn/fullScreen}}"
                    press=".toggleFullScreen"/>
        </semantic:exitFullScreenAction>

    </semantic:SemanticPage>

</mvc:View>